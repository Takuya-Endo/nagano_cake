<h1>商品一覧</h1>

<% @items.each do |item| %>

  <div>
    <span><%= item.id %></span>
    <span><%= link_to item.name, admin_item_path(item.id) %></span>
    <span><%= item.price %></span>
    <span>ジャンル</span>
    <span>販売ステータス</span>
  </div>

<% end %>